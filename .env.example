VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — only needed if/when the FASS FILL AI backend is deployed.
# Leave blank to keep AI buttons hidden; the rest of the app works without it.
VITE_API_URL=
